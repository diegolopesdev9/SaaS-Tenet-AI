No arquivo frontend/src/pages/admin/Agencias.jsx, adicione o campo nicho no formulário de criar agência.

1. ENCONTRE o estado inicial do formulário (algo como useState com nome, email, etc) e ADICIONE nicho:

const [formData, setFormData] = useState({
  nome: '',
  email: '',
  instance_name: '',
  whatsapp_phone_id: '',
  admin_nome: '',
  admin_email: '',
  admin_senha: '',
  nicho: 'sdr'  // ADICIONE ESTA LINHA
})

OU se o estado for diferente, adicione nicho: 'sdr' no objeto inicial.

2. ENCONTRE onde os campos do formulário são renderizados (inputs de Nome, Email, etc) e ADICIONE após o campo Instance Name:

            {/* Campo Nicho */}
            <div>
              <label className="block text-sm font-medium text-gray-700 mb-1">
                Nicho do Agente *
              </label>
              <select
                value={formData.nicho || 'sdr'}
                onChange={(e) => setFormData({...formData, nicho: e.target.value})}
                className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                required
              >
                <option value="sdr">🎯 Tenet SDR - Qualificação de Leads</option>
                <option value="suporte">🛠️ Tenet Suporte - Atendimento Técnico</option>
                <option value="rh">👥 Tenet RH - Recursos Humanos</option>
                <option value="vendas">💰 Tenet Vendas - Atendimento Comercial</option>
                <option value="custom">⚙️ Tenet Custom - Personalizado</option>
              </select>
            </div>

3. Na tabela de listagem de agências, ADICIONE uma coluna para mostrar o nicho:

No header da tabela, adicione:
<th className="py-3 px-4 text-left text-xs font-medium text-gray-500 uppercase">Nicho</th>

Na linha de cada agência, adicione a célula:
<td className="py-3 px-4">
  <span className={`px-2 py-1 text-xs font-medium rounded-full ${
    agencia.nicho === 'sdr' ? 'bg-blue-100 text-blue-800' :
    agencia.nicho === 'suporte' ? 'bg-green-100 text-green-800' :
    agencia.nicho === 'rh' ? 'bg-purple-100 text-purple-800' :
    agencia.nicho === 'vendas' ? 'bg-orange-100 text-orange-800' :
    'bg-gray-100 text-gray-800'
  }`}>
    {agencia.nicho?.toUpperCase() || 'SDR'}
  </span>
</td>

NÃO altere nenhum outro código.